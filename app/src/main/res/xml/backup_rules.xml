<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!--
        Only the user's library metadata and reading progress are backed up.
        Session state, cache, and downloaded chapter bodies are excluded
        implicitly because they are not part of this allowlist.
    -->
    <include
        domain="database"
        path="openstory.db" />
</full-backup-content>

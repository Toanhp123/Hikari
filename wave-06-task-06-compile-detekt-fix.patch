diff --git a/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingSheet.kt b/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingSheet.kt
index 44d2216..d7c1294 100644
--- a/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingSheet.kt
+++ b/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingSheet.kt
@@ -117,4 +117,6 @@ private fun ContentMatchDecision.displayName(): String = when (this) {
     ContentMatchDecision.REJECT -> "Reject"
 }
 
-private fun Double.asPercent(): String = String.format(Locale.ROOT, "%.0f%%", this * 100.0)
+private const val PERCENT_MULTIPLIER = 100.0
+
+private fun Double.asPercent(): String = String.format(Locale.ROOT, "%.0f%%", this * PERCENT_MULTIPLIER)
diff --git a/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingViewModel.kt b/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingViewModel.kt
index ae8d795..34c9097 100644
--- a/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingViewModel.kt
+++ b/feature/catalog/src/main/kotlin/app/openstory/catalog/ui/mapping/MappingViewModel.kt
@@ -171,12 +171,13 @@ private fun PendingCandidate.toUiModel() = MappingCandidateUiModel(
 )
 
 private fun ContentMatchExplanation.evidenceLabels(): List<String> = buildList {
+    val currentAuthorSimilarity = authorSimilarity
     if (directEvidence) add("Direct mapping")
     add("Title ${title.similarity.asPercent()}")
     when {
         authorConflict -> add("Author conflict")
-        authorSimilarity == null -> add("Author evidence missing")
-        else -> add("Authors ${authorSimilarity.asPercent()}")
+        currentAuthorSimilarity == null -> add("Author evidence missing")
+        else -> add("Authors ${currentAuthorSimilarity.asPercent()}")
     }
     when {
         contentTypeConflict -> add("Content type conflict")
@@ -185,4 +186,6 @@ private fun ContentMatchExplanation.evidenceLabels(): List<String> = buildList {
     }
 }
 
-private fun Double.asPercent(): String = String.format(Locale.ROOT, "%.0f%%", this * 100.0)
+private const val PERCENT_MULTIPLIER = 100.0
+
+private fun Double.asPercent(): String = String.format(Locale.ROOT, "%.0f%%", this * PERCENT_MULTIPLIER)
